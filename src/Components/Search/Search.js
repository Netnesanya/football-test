import React from 'react';
import {TextField} from "@mui/material";

const Search = () => {
    return (
        <TextField

            id={"standard"}
            type={'search'}
            label={'search'}
        />
    );
};

export default Search;