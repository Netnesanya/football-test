import React from 'react';

const Competition = (props) => {

    return (
        <div>
           {props.id}  {props.name}  {props.area}
            aa
        </div>
    );
    };

export default Competition;